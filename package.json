{
  "name": "assinatura-partido-mjp",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=neutral --outfile=dist/index.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.20.0",
    "wrangler": "^3.0.0"
  }
}
